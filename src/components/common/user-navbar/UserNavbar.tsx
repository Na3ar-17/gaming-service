import { NextPage } from 'next'
import styles from './UserNavbar.module.scss'

const UserNavbar: NextPage = () => {
  return <main className={styles.container}>User NavBar</main>
}

export default UserNavbar
